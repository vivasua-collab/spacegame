# Чекпоинт: MERGE — слияние ветки r27-chemistry (R-26 карусель + R-27 химия) в main (R-23…R-30)

**Дата:** 2026-09-03 (Europe/Moscow)
**Фаза:** интеграция параллельных линий разработки
**Статус:** complete

## Контекст расхождения

После сбоя сессии возникли ДВЕ параллельные линии от общего предка `f68be41` (R-SHIPS-DATA):

| Линия | HEAD | Содержимое |
|---|---|---|
| **origin/main** | `a22e9dc` (R-30) | R-STARS-DATA, R-23 синергия+research split, R-24 энергия+снос, R-25, R-26 гравитация+gzip-транспорт, R-27 автопереработка+газовый склад v3.1, R-28 склад+сейв v2, R-29 ленивые залежи+сейв v3, R-30 снятие заглушек v1/v2. 567 тестов |
| **локальный main = origin/r27-chemistry** | `8c67a95` (R-27 химия) | R-26 карусель производства (баги 3–4) + плоские меню постройки (баги 1–2) + R-27 химия: Уголь→C+Шлак, Песок, Каолин, Вода — единственный источник H/O. 417 тестов |

Директива владельца: «аккуратно выполнить слияние… если не возможно слияние, за
основу берётся код с ветки main и в него внедряется функционал корректировки химии».

## Правила слияния (применённые)

1. **Инфраструктура — основа origin/main**: формат сейва v3 (ленивые залежи,
   словарь id, строгий fmt:3), склад v3.1 (4 склада + резервы + единый список
   «количество / резерв»), синергия v2 (adjacency-движок: power_grid/power_boost/
   mining_cluster/processing_speed), автопереработка R-27, гравитация, gzip-транспорт.
2. **Портировано из r27-chemistry**: карусель производства (assignQueueTasks +
   collectProcessorInstances + ProductionItem.assignedTo + липкие назначения +
   приоритет специализации в общей карусели), плоские меню постройки, химия
   (ore-specs/recipes/processing-chains/crafted-materials + Шлак/processor-recipe-categories/
   ice-generator/generate-resources: вода вместо залежей атмосферных элементов),
   вкладка «Переработка» в справке, carousel-simulation скрипт.
3. **Дубликаты функционала — победила сторона origin** (каноничнее и новее):
   - солнечная синергия R-26 (SOLAR_SYNERGY_PER_NEIGHBOR/countSolarNeighbors) →
     замещена typed power_boost (R-SYNERGY v2, +5%/потребитель со стекингом);
   - объединение вкладок склада R-26 (ResourcePanel) → замещено R-28-списком
     (приоритеты резервов, топливо флотов, 4 склада);
   - заглушки v1/v2 R-26 → замещены R-30 (строгий контракт fmt:3, v2-кодек удалён).

## Ключевые интеграции в engine.ts

- `processProductionQueue`: карусель R-26 + **R-SYNERGY §processing** — множитель
  скорости считается по ТОЧНОМУ исполнителю (`instance.key` = hexIndex), а не по
  «первому попавшемуся» зданию как в однозадачной версии origin.
- `recalcEnergyBalance`: версия origin (getBuildingEnergyOutput/Consumption +
  power_grid/power_boost) — локальная солнечная адаптация удалена.
- Автопереработка origin (BASE_CONSTRUCTION_RECIPE_IDS) работает поверх новой
  химии: Уголь→C 8+Шлак 2, Каолин→Al 2.1+Si 2.2+O 5.6+H 0.2, Песок→Si 4.7+O 5.3.

## Разрешённые конфликты (7 файлов)

- `src/economy/engine.ts` — 6 регионов (карусель+синергия, энергия v2, хелперы)
- `src/stores/game-store.ts` — миграции полей (migrateResearchState/migratePlanet)
  удалены: слитая deserializeGameState (v3-строгая) их не вызывает; старых сейвов нет
- `src/components/game/building-dialog.tsx` — 5 регионов: база origin (AlertDialog
  снос/понижение, реальная энергия R-24, панель R-SYNERGY) + плоский список R-26
- `src/components/game/planet-view.tsx` — 4 региона: WarehousePanel R-28 целиком
- `tests/serialization.test.ts` — комментарии строгого режима (поведение совпало)
- `scripts/save-size-analysis.ts` — версия origin (v3-кодек, `bun run save:size`)
- `worklog.md` — обе истории сохранены

## Верификация

- **Гейты:** lint 0 errors/48 warnings (=базлайн); tsc src/ 91 (базлайн origin
  123 — улучшилось); `bun test` **567/567**; `validate:all` 4/4 (recipes 75,
  buildings, ships, stars+grids).
- **Карусель-симуляция:** сценарии A (2 автоповтора параллельно), B (2+3 →
  лимит слотов), C (специализация вне очереди) — все PASS.
- **Тест химии:** auto-processing.test №3 переписан под новую химию (Si
  суммируется из песка+каолина; +Шлак/H/O побочные) — единственный фейл слияния.
- **Браузер (agent-browser, E2E, seed 958305):**
  - новая игра → колонизация Nu Tauri I (вулканическая);
  - гекс-диалог: «Гекс поверхности #3 · Поверхность», **0 вкладок**, НЕТ
    космопорта/верфи/орбитальных, газовый экстрактор ЕСТЬ (баги 1–2);
  - 2 переработчика + солнечная станция → очередь: «Переработка угля → гекс #3
    (автоповтор)» + «Плавка железа → гекс #6» — **обе прогрессируют параллельно**
    с бейджами исполнителей (баг 3);
  - склад: единый список «Хранилище · количество / резерв», 4 склада
    (Рудный/Элементы/Высокотех/Газовый), RED ниже резерва / GREEN выше;
    ресурсы: **Уголь 3/40, Песок 50/50, Каолин 0/50, Вода (лёд) 44/50, Шлаг 10/50,
    Углерод 96/24** — вся химия R-27 в игре;
  - справка → вкладка «Переработка»: «Как устроена переработка», «Кислород и
    водород не встречаются в виде отдельных залежей — это же вода!», цепочки
    «C 8 Sl 2», «H 1.1 O 8.9», Каолин;
  - Save → полная перезагрузка → Load: колония, очередь (2), бейджи, прогресс,
    химия восстановлены (fmt:3); тест-сейв удалён из БД;
  - консоль/dev.log без ошибок.

## Проблемы

- Инцидент при сверке tsc-базлайна: последовательность stash→checkout→pop
  «заморозила» состояние MERGE_HEAD (резолюции сохранились полностью в
  worktree+index) — merge-commit собран вручную через `git commit-tree`
  с двумя родителями (`8c67a95` + `a22e9dc`), топология истории корректна.
- Генерация залежей отличается от обеих родительских линий (химия меняет пул
  элементов) — ожидаемо; совместимость старых сейвов не требуется (R-30).

## Следующие шаги

- Шлак → бетон (рецепт + применение в зданиях) — следующий этап.
- Ледодобыча CO₂/CH₄/NH₃-льдов (ice_harvester) — в планах.
- Docs-синк: 33-chemistry/34-ores под новую химию (бэклог аудита).

## Изменённые файлы

- Слияние: 55+ файлов (см. `git show --stat` merge-коммита)
- Конфликты: engine.ts, game-store.ts, building-dialog.tsx, planet-view.tsx,
  serialization.test.ts, save-size-analysis.ts, worklog.md
- tests/economy/auto-processing.test.ts — химия R-27 в тесте №3
- checkpoints/09_03_merge_r26_r27_to_main.md, worklog.md, README.md (счётчик 44)
