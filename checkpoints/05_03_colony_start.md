# Чекпоинт: Colony Start + Generator Refactor

**Дата:** 2026-05-04 02:30 MSK (обновлено — рефакторинг генератора завершён)
**Фаза:** 2 — UI/UX + Gameplay Start
**Статус:** in_progress

## Выполненные задачи
- G-24: PRNG derive() исправлен (6/6 тестов PASS)
- G-25: Размер 'large' теперь генерируется (86 планет)
- G-27: Океанические планеты 7.9% (масштабирование орбит)
- G-29: Минимальная гравитация ГГ 0.94g
- **Аудит v2:** Все G-01..G-18 подтверждены как исправленные в коде
- ✅ Colony Hub + UI колонизации (чекпоинт 05_03_colony_hub_plan.md — completed)
- ✅ G-30: Парниковый эффект = % от T_eq (масштабируется с расстоянием)
- ✅ G-31: Имя планеты содержит тип (TYPE_NAMES)

## Аудит генерации (v2 — ключевые выводы)

### ✅ Все критические баги исправлены
Все 18 багов (G-01..G-18) из v1 аудита подтверждены как исправленные в текущем коде.

### 🟡 Осознанные отклонения от спецификации (4 шт.)
1. **G-25:** Rocky max radius=10000 (спец: 7000), Oceanic max=11000 (спец: 8000) — для "large" сетки
2. **G-27:** Орбитальный радиус масштабируется от hzCenter — для M/K карликов
3. **G-29:** Gas giant density min=1.2 (спец: 0.8) — для мин. гравитации ~0.9g
4. **G-19:** Вариация звёзд ±20% вместо subclass-based интерполяции — упрощение MVP

### 🔴 Внутренние противоречия в спецификации (7 шт.)
В `docs/03-planets.md` §1.2 (описания типов) и §2.4 (таблица вероятностей) содержат **разные** распределения атмосферы для ВСЕХ 7 типов планет. Код следует §2.4. Рекомендуется обновить §1.2.

### 🟡 Мелкие расхождения (6 шт.)
- diskFraction в конфиге не используется (захардкожено 0.20)
- lifeChance/size/hexCount в PLANET_TYPES не используются (legacy поля)
- Орбитальный радиус для двойных: фиксированные ограничения вместо формул из спец
- Астероидные поля: бонус +1 вместо процентной вероятности
- Composition атмосферы всегда `[]`

### Полный аудит: `doc_temp/galaxy-generation-audit.md`

## Текущие задачи
1. ~~Добавить размер и тип планеты в отображение названия~~ ✅ (badges в UI + TYPE_NAMES в имени)
2. ~~Создать здание колонизации (colony_hub)~~ ✅ (05_03_colony_hub_plan.md)
3. ~~Реализовать механику автоматической постановки colony_hub~~ ✅
4. ~~Подготовить план рефакторинга генератора (оркестратор + модули)~~ ✅ (реализовано)
5. Обновить §1.2 в 03-planets.md для согласованности с §2.4

## Проблемы
- ~~Нет здания колонизации~~ ✅ Исправлено (colony_hub)
- ~~Нет UI-флоу для старта~~ ✅ Исправлено (фаза colonization + кнопка)
- ~~Немонотонная температура~~ ✅ Исправлено (G-30: парник = % от T_eq)
- ~~Генератор generator.ts слишком большой (~1200 строк) — нужен рефакторинг~~ ✅ Рефакторинг завершён (оркестратор + 5 модулей + gen-context)
- Внутренние противоречия в docs/03-planets.md (§1.2 vs §2.4) — нужны правки в документации

## Следующие шаги
- ~~Реализовать colony_hub здание + механику~~ ✅
- ~~Добавить UI-флоу колонизации~~ ✅
- ~~Рефакторинг генератора на модули~~ ✅
- Обновить §1.2 в 03-planets.md для согласованности с §2.4

## Изменённые файлы
- doc_temp/galaxy-generation-audit.md (обновлён до v2)

## План рефакторинга генератора

### Текущая структура (1 файл ~1200 строк)
```
src/galaxy/generator.ts — ВСЁ в одном файле
  generateGalaxy()
  generateSpiralPositions()
  generateSystem()
  generatePlanet()
  calculatePlanetTemperature()
  generateAtmosphere()
  generateLife()
  selectPlanetType()
  assignResourceDeposits()
  aggregateResourceDeposits()
  generateJumpPoints()
  ensureConnectivity()
  generateSystemName()
```

### Целевая структура (оркестратор + 5 модулей) ✅ РЕАЛИЗОВАНО
```
src/galaxy/
  index.ts              — экспорт generateGalaxy + типы
  generator.ts          — ОРКЕСТРАТОР (~90 строк) ✅
  gen-context.ts        — общий контекст (genId, usedNames, resetGenContext) ✅
  generate-positions.ts — генерация позиций (спираль, ядро, диск, ореол) ✅
  generate-systems.ts   — генерация звёздных систем (звёзды, компаньоны, имена) ✅
  generate-planets.ts   — генерация планет (тип, атмосфера, температура, жизнь) ✅
  generate-resources.ts — генерация ресурсов (deposits, ultra-rare) ✅
  generate-jump-points.ts — JP + связность ✅
  hex-grid.ts           — гексагональная сетка (без изменений)
```

### Граф зависимостей (DAG, без циклов)
```
generator.ts (оркестратор)
  ├── gen-context.ts
  ├── generate-positions.ts
  ├── generate-systems.ts
  │     ├── gen-context.ts
  │     └── generate-planets.ts
  │           ├── gen-context.ts
  │           ├── generate-resources.ts
  │           └── hex-grid.ts
  └── generate-jump-points.ts
        └── gen-context.ts
```

### Схема генерации (согласовано с пользователем)
1. Задаётся количество звёзд (systemCount)
2. Генерируются «гнёзда» — позиции согласно схеме галактики (ядро/рукава/диск/ореол)
3. Генерируется набор звёзд по правилам распределения → размещаются в гнёзда
4. Для каждой звезды по классу генерируются планеты
5. Генерация ультра-редких ресурсов + распределение по планетам
6. Финализация (JP, связность, стартовая система)
