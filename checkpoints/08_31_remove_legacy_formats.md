# Чекпоинт: снятие заглушек совместимости сейвов v1/v2

**Дата:** 2026-08-31 16:05
**Фаза:** Сериализация (save/load pipeline)
**Статус:** complete

## Контекст

Владелец: «На текущем этапе совместимость сейвов не требуется. Нечего
захламлять код. Старые сейвы я потер, потому "заглушки" от V1 и V2 нужно
убрать.» Таблица `GameSave` пуста (владелец стёр сейвы; проверено запросом
к БД до начала работ). План: `plans/2026-08-31-task30-remove-legacy-formats.md`
(Task ID 30).

## Выполненные изменения

- **Удалён `src/lib/save-format-v2.ts`** (275 строк): `compactSaveV2`,
  `expandSaveV2`, `isSaveFormatV2`, `SAVE_FORMAT_VERSION`. Хелперы
  `round3` / `roundSig4` / `TIER_TO_IDX` / `TIER_FROM_IDX` перенесены в
  `src/lib/save-format-v3.ts` (логика округления/тиров кодека v3).
- **`src/lib/save-format-v3.ts`**: удалены `migrateLegacyDepositFlags`
  и `migrateBody` (миграция флагов нужна только сейвам v1/v2);
  `expandSaveV3` — строгий контракт: `fmt !== 3` → явная ошибка
  «Неподдерживаемый формат сейва» (вместо молчаливой порчи состояния
  кортежами v2); шапка-документация переписана (R-30: v3 — единственный
  формат).
- **`src/stores/game-store.ts`**: убраны импорты `expandSaveV2` /
  `migrateLegacyDepositFlags`; `deserializeGameState` — единственный путь
  `expandSaveV3(raw)` (fmt-детект удалён); комментарии обновлены.
- **`src/app/page.tsx`**: `handleLoad` теперь показывает тост
  «Не удалось загрузить сейв…» (destructive) при `loadGame === false` —
  раньше отказ был виден только в консоли.
- **Комментарии**: `generate-resources.ts` (materializePlanetDeposits),
  `economy/engine.ts` (colonizePlanet) — убраны упоминания v1/v2-пути.
- **`INSTRUCTIONS.md`**: fmt-раздел — только v3; v1/v2 отклоняются.
- **Тесты**: `tests/save-format-v2.test.ts` удалён (−11); в
  `tests/save-format-v3.test.ts` тест 9 заменён на «старые форматы
  (fmt:2 / без fmt) отклоняются явной ошибкой»; в
  `tests/serialization.test.ts` fixtures тестов 3/7 обёрнуты `fmt: 3`
  (проверяют живые защитные пути внутри валидного v3).

## Гейты

- bun test: **567/567** (578 − 11 удалённых v2-тестов), 0 fail.
- lint: 0 errors / 48 warnings (= базлайн).
- tsc: дельта к HEAD = 0 (157 = 157 — базлайн дрейфовал до R-30, правки
  нейтральны; 2 упоминания в save-format-v3.test.ts существовали до R-30).
- validate:all: 4/4 ✅.

## Браузерная верификация

- Новая игра (seed 42) → колонизация Eta Eridani I → Save → БД:
  **fmt=3, 2.75 МБ, dict=116** → полная перезагрузка → Load → колония,
  61 гекс, пул «Ресурсы (60)», склад «количество / резерв» восстановлены.
- E2E-отказ старого формата: тестовый v2-сейм вставлен в БД → Load →
  `Load failed: Неподдерживаемый формат сейва: fmt=2` в консоли,
  видимый тост, без крэша; тестовый сейв удалён через UI.
- Чистая перезагрузка: ошибок страницы/консоли нет; игра на x1 идёт.

## Значение для проекта

- Пайплайн загрузки сейвов линеен: один формат, один декодер, явный
  контракт (`fmt:3`), честная ошибка вместо тихой порчи.
- −1 файл (~275 строк) и −11 тестов кодека v2; хелперы живут там, где
  используются.
- DEV-сервер оставлен работающим (сейв Galaxy #42 fmt:3 в Load-списке
  для превью владельца).
